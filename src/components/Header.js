import React from 'react';
import styled from 'styled-components';

const Header = styled.header`
  display: block;
`

export default () =>
  <Header>
    <h1>Databend Cloud Beautiful Icons</h1>
  </Header>
